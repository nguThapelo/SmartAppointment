// Placeholder landing page — replaced by the real UI in Phase 4.
export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 px-6">
      <h1 className="text-3xl font-semibold">SmartAppointment</h1>
      <p className="text-slate-600">
        Appointment booking for clients and providers — on the web, on WhatsApp, and through an AI assistant.
      </p>
      <p className="text-sm text-slate-500">The new interface is being rebuilt. The API is live under /api.</p>
    </main>
  );
}
